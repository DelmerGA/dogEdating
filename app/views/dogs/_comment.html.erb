<h4>Comment: </h4>

<%#= Dog.find(User.find(Comment.find.user_id)).name, "/dogs/#{Dog.find(User.find(Comment.find.user_id)).id}" %>
<%= comment.body %><br />
<%= link_to "Edit Comment", edit_comment_path(comment), method: 'get' %><br />

